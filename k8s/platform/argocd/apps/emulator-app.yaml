apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: emulator
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/Tim-McCann/homelab.git
    targetRevision: HEAD
    path: k8s/apps/emulator
  destination:
    server: https://kubernetes.default.svc
    namespace: emulator
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
